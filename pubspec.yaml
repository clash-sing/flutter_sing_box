name: flutter_sing_box
description: "A Flutter plugin for sing-box, supporting remote profile import, Clash API, and VPN service management for Android and iOS."
version: 1.1.6
homepage: https://github.com/clash-sing/flutter_sing_box
repository: https://github.com/clash-sing/flutter_sing_box.git

environment:
  sdk: ^3.12.0
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  mmkv: ^2.4.0
  dio: ^5.10.0
  package_info_plus: ^10.1.0
  json_annotation: ^4.12.0
  collection: ^1.19.1
  device_info_plus: ^13.1.0
  path_provider: ^2.1.5
  path: ^1.9.1
  yaml: ^3.1.3
  freezed_annotation: ^3.1.0
  crypto: ^3.0.6
  win32: ^6.3.0
  ffi: ^2.2.0
  material_ui: ^1.0.0
  cupertino_ui: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.10.0
  json_serializable: ^6.14.0
  freezed: ^3.2.5

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.clashsing.flutter_sing_box
        pluginClass: FlutterSingBoxPlugin
        dartPluginClass: MethodChannelFlutterSingBox
      windows:
        pluginClass: FlutterSingBoxPluginCApi
        dartPluginClass: FlutterSingBoxWindows
      ios:
        pluginClass: FlutterSingBoxPlugin
        dartPluginClass: MethodChannelFlutterSingBox

  # To add assets to your plugin package, add an assets section, like this:
  assets:
    - assets/configs/
    - path: assets/windows/sing-box.exe
      platforms: [ windows ]
    - path: assets/windows/libcronet.dll
      platforms: [ windows ]
    - path: assets/windows/clash_sing_helper.exe
      platforms: [ windows ]
#    - assets/.local/

  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
